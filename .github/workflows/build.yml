name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - '*.*.*'
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.github/**'
      - '!.github/workflows/build.yml'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g., 1.0.0)'
        required: false
        default: ''

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Linux packages
        run: npm run package:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: linux-builds
          path: |
            release/*.AppImage
            release/*.deb
            release/*.rpm
          retention-days: 5

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Windows package
        run: npm run package:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-builds
          path: release/*.exe
          retention-days: 5

  build-windows-legacy:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install legacy-compatible packages
        run: |
          npm install --save-dev electron@22.3.27 --ignore-scripts
          npm install --save electron-store@8.1.0 --ignore-scripts

      - name: Build main process (legacy bundle)
        run: node --max-old-space-size=4096 ./node_modules/webpack/bin/webpack.js --config webpack.main.legacy.js

      - name: Build renderer (legacy compatible)
        run: node --max-old-space-size=4096 ./node_modules/webpack/bin/webpack.js --config webpack.renderer.legacy.js

      - name: Package Windows legacy
        run: npx electron-builder --win --config electron-builder-legacy.json
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows Legacy artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-legacy-builds
          path: release-legacy/*.exe
          retention-days: 5

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build macOS packages
        run: npm run package:mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: macos-builds
          path: release/*.zip
          retention-days: 5

  release:
    needs: [build-linux, build-windows, build-windows-legacy, build-macos]
    runs-on: ubuntu-latest
    if: success()
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version from package.json
        id: package-version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check-tag
        run: |
          if git rev-parse "${{ steps.package-version.outputs.version }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create and push tag
        if: steps.check-tag.outputs.exists == 'false' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "${{ steps.package-version.outputs.version }}" -m "Release ${{ steps.package-version.outputs.version }}"
          git push origin "${{ steps.package-version.outputs.version }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.package-version.outputs.version }}
          name: Marix ${{ steps.package-version.outputs.version }}
          files: |
            artifacts/linux-builds/*
            artifacts/windows-builds/*
            artifacts/windows-legacy-builds/*
            artifacts/macos-builds/*
          draft: false
          prerelease: false
          generate_release_notes: false
          body: |
            ## Marix SSH Client ${{ steps.package-version.outputs.version }}
            
            ### Downloads
            | Platform | File | Supported OS |
            |----------|------|--------------|
            | **Linux** | AppImage, DEB, RPM | Ubuntu 18.04+, Debian 10+, Fedora 32+ |
            | **Windows** | EXE installer | Windows 10/11 |
            | **Windows (Legacy)** | EXE installer | Windows 7/8/Server 2012 |
            | **macOS** | ZIP (Universal) | macOS 10.15+ (Intel & Apple Silicon) |
            
            ### Installation
            
            **Windows Legacy** (Win 7/8/Server 2012):
            - Download the file ending with `-legacy-win.exe`
            - Requires Windows 7 SP1 or later
            
            **macOS**:
            1. Download and extract the ZIP file
            2. Right-click Marix.app → Open (bypass Gatekeeper)
            3. If blocked: System Settings → Privacy & Security → Open Anyway
            
            ---
            
            📋 **[View Full Changelog](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md)**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
