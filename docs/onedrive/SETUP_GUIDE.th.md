# คู่มือการตั้งค่า Microsoft OneDrive OAuth2

คู่มือนี้จะช่วยคุณตั้งค่าการยืนยันตัวตน OneDrive OAuth2 สำหรับ Marix

## ข้อกำหนดเบื้องต้น

- บัญชี Microsoft ส่วนตัว
- เข้าถึง [Azure Portal](https://portal.azure.com)

## ขั้นตอนที่ 1: ลงทะเบียนแอปพลิเคชันใหม่

1. ไปที่ [Azure Portal - การลงทะเบียนแอป](https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade)
2. คลิก **"การลงทะเบียนใหม่"**
3. กรอกรายละเอียด:
   - **ชื่อ**: `Marix SSH Client`
   - **ประเภทบัญชีที่รองรับ**: เลือก **"บัญชี Microsoft ส่วนบุคคลเท่านั้น"**
   - **URI การเปลี่ยนเส้นทาง**: เว้นว่างไว้ (จะเพิ่มในขั้นตอนที่ 3)
4. คลิก **"ลงทะเบียน"**

## ขั้นตอนที่ 2: บันทึก ID แอปพลิเคชัน

หลังจากลงทะเบียน:
1. คัดลอก **ID แอปพลิเคชัน (ไคลเอนต์)**
2. เก็บไว้ในที่ปลอดภัย

## ขั้นตอนที่ 3: กำหนดค่าการยืนยันตัวตน (พอร์ตสุ่ม)

Marix ใช้การเปลี่ยนเส้นทาง **RFC 8252** กับพอร์ตสุ่มเพื่อความปลอดภัย

1. คลิก **"การยืนยันตัวตน"**
2. ใน **"การกำหนดค่าแพลตฟอร์ม"** คลิก **"เพิ่มแพลตฟอร์ม"**
3. เลือก **"แอปพลิเคชันมือถือและเดสก์ท็อป"**
4. ⚠️ **สำคัญ**: เพิ่ม URI การเปลี่ยนเส้นทางหลายรายการ:
   ```
   http://127.0.0.1/callback
   http://127.0.0.1:8888/callback
   http://127.0.0.1:8889/callback
   http://127.0.0.1:8890/callback
   http://127.0.0.1:8891/callback
   http://127.0.0.1:8892/callback
   http://127.0.0.1:8893/callback
   http://127.0.0.1:8894/callback
   http://127.0.0.1:8895/callback
   http://127.0.0.1:8896/callback
   http://127.0.0.1:8897/callback
   ```
5. เลือก: `https://login.microsoftonline.com/common/oauth2/nativeclient` ด้วย
6. ใน **"การตั้งค่าขั้นสูง"**:
   - ตั้ง **"อนุญาตโฟลว์ไคลเอนต์สาธารณะ"** เป็น **ใช่** ⚠️
7. คลิก **"บันทึก"**

## ขั้นตอนที่ 4: กำหนดค่าสิทธิ์ API

1. คลิก **"สิทธิ์ API"**
2. คลิก **"เพิ่มสิทธิ์"**
3. เลือก **"Microsoft Graph"** → **"สิทธิ์ที่มอบหมาย"**
4. เพิ่ม:
   - ✅ `Files.ReadWrite`
   - ✅ `User.Read`
   - ✅ `offline_access`
5. คลิก **"เพิ่มสิทธิ์"**

## ขั้นตอนที่ 5: สร้างไฟล์ข้อมูลรับรอง

**สำหรับการพัฒนาในเครื่อง**: แก้ไข `src/main/services/onedrive-credentials.json`:

```json
{
  "client_id": "ID_ไคลเอนต์ของคุณ"
}
```

**สำหรับ CI/CD builds**: ใช้ GitHub Secrets (ดูด้านล่าง)

## ขั้นตอนที่ 6: กำหนดค่าใน Marix

### ตัวเลือก A: การพัฒนาในเครื่อง

1. สร้างไฟล์ `onedrive-credentials.json` ในโฟลเดอร์ `src/main/services/`
2. **สำคัญ**: เพิ่มใน `.gitignore`:
```
src/main/services/onedrive-credentials.json
```

### ตัวเลือก B: CI/CD ด้วย GitHub Secrets (แนะนำ)

1. ไปที่ GitHub repository → **Settings** → **Secrets and variables** → **Actions**
2. เพิ่ม secret นี้:
   - `ONEDRIVE_CLIENT_ID`: OAuth Application (Client) ID ของคุณ
3. workflow จะ inject credentials โดยอัตโนมัติระหว่าง build

## ขั้นตอนที่ 7: สร้างและทดสอบ

```bash
npm run build
npm start
```

## หมายเหตุด้านความปลอดภัย

- ✅ **PKCE**: ไม่ต้องการ client_secret
- ✅ **RFC 8252**: การเปลี่ยนเส้นทาง loopback กับพอร์ตสุ่ม
- ✅ **การเข้ารหัส E2E**: Argon2id + AES-256-GCM
- 📁 **ตำแหน่งที่เก็บ**: `/Marix/backup.marix`
- ใช้ **GitHub Secrets** สำหรับ CI/CD builds เพื่อปกป้อง credentials
